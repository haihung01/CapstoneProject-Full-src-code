package com.example.triptix.Service.Impl;

import com.example.triptix.DTO.Coordinates;
import org.springframework.stereotype.Service;

@Service
public class GeocodingService {
    public Coordinates getCoordinates(String address) {
        // Thực hiện lấy tọa độ từ địa chỉ và trả về đối tượng Coordinates
        // Sử dụng thư viện hoặc dịch vụ định vị của bạn ở đây
        // (Dùng RestTemplate, WebClient, hoặc các thư viện khác)
        // ...

        return null; // Thay bằng mã lấy tọa độ thực tế
    }
}
